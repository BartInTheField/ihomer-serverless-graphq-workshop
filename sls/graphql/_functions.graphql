type Query {
    # Wallets
    listWallets: [Wallet]
    getWallet(walletId: ID!): Wallet
    # Coins
    listCoins: [Coin]
    getCoin(coinId: ID!): Coin
}

type Mutation {
    resetDatabase: Boolean
}
