type Query {
    # Wallets
    listWallets: [Wallet]
    getWallet(walletId: ID!): Wallet
    # Coins
    listCoins: [Coin]
    getCoin(coinId: ID!): Coin
    # Profiles
    listProfiles: [Profile]
}

type Mutation {
    resetDatabase: Boolean
}
